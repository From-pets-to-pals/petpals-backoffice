# Node.js with Angular
# Build a Node.js project that uses Angular.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '18.14'
  displayName: 'Install Node.js'
- script: |
    cd front
    npm install -g @angular/cli
    npm install
    ng build
  displayName: 'npm install and build'
- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: '$(Build.SourcesDirectory)/front/dist/front'
    includeRootFolder: true
    archiveType: 'zip'
    archiveFile: '$(Build.ArtifactStagingDirectory)/petpals-backoffice-$(Build.BuildId).zip'
    replaceExistingArchive: false

- task: AzureStaticWebApp@0
  inputs:
    output_location: '$(Build.SourcesDirectory)/front/dist/front'
    skip_app_build: true
    skip_api_build: true
    verbose: true
    azure_static_web_apps_api_token: 'a910747c0549fc94efe753d0bec81d6378caa4b9cfb8e52eec5c07b9d1f0191d5-62476482-e844-4bf4-ab09-ac0effabaf9e003420128'


